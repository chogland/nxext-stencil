import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import Readme from './readme.md';
import * as RdsCheckbox from '../../../../dist/collection/components/rds-form-elements/rds-checkbox/rds-checkbox';

<Meta
  title="Components/Form Elements/Checkbox/Stories"
  component={RdsCheckbox}
  parameters={{
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/fws0J5pWjrviPYy0MLdEa3/RDS-Sticker-Sheet?node-id=48%3A17906',
    },
  }}
  argTypes={{
    label: {
      control: { type: 'text' },
      description: 'The text which is shown',
    },
    checked: {
      control: { type: 'boolean' },
      description: 'If true, the checkbox is checked',
    },
    disabled: {
      control: { type: 'boolean' },
      description: 'If true, the checkbox is displayed as disabled',
    },
  }}
/>

export const Template = args => <rds-checkbox {...args}></rds-checkbox>;

export const CardTemplate = args => (
  <rds-checkbox {...args}>
    <rds-flex slot="card-content" direction="column" gap="xl">
      <rds-text spacing="none" size="sm" color="gray-500">
        Last message sent 2 weeks ago
      </rds-text>
      <rds-text spacing="none" size="sm" color="gray-900">
        1200 users
      </rds-text>
    </rds-flex>
  </rds-checkbox>
);

export const WithSubLabel = args => (
  <rds-checkbox {...args}>
    <rds-sub-label slot="sub-label">I am a sub-label component</rds-sub-label>
  </rds-checkbox>
);

# Checkbox

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Checkbox',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Checked

<Canvas>
  <Story
    name="Checked"
    args={{
      label: 'Checkbox',
      checked: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      label: 'Checkbox',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### With Sub-Label

<Canvas>
  <Story
    name="With Sub-Label"
    args={{
      label: 'With Sub-Label',
    }}
  >
    {WithSubLabel.bind({})}
  </Story>
</Canvas>

### Small Card

<Canvas>
  <Story
    name="Small Card"
    args={{
      label: 'Checkbox',
      type: 'small-card',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Small Card: Checked

<Canvas>
  <Story
    name="Small Card: Checked"
    args={{
      label: 'Checkbox',
      type: 'small-card',
      checked: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Small Card: Disabled

<Canvas>
  <Story
    name="Small Card: Disabled"
    args={{
      label: 'Checkbox',
      type: 'small-card',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Card

<Canvas>
  <Story
    name="Card"
    args={{
      label: 'Existing Customers',
      type: 'card',
    }}
  >
    {CardTemplate.bind({})}
  </Story>
</Canvas>

### Card: Checked

<Canvas>
  <Story
    name="Card: Checked"
    args={{
      label: 'Existing Customers',
      type: 'card',
      checked: true,
    }}
  >
    {CardTemplate.bind({})}
  </Story>
</Canvas>

### Card: Disabled

<Canvas>
  <Story
    name="Card: Disabled"
    args={{
      label: 'Existing Customers',
      type: 'card',
      disabled: true,
    }}
  >
    {CardTemplate.bind({})}
  </Story>
</Canvas>

_Built for Resilience Design System @ FM Global_
