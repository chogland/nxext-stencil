import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import Readme from './readme.md';
import * as RdsRadioButtonGroup from '../../../../dist/collection/components/rds-form-elements/rds-radio-button-group/rds-radio-button-group';

<Meta title="Components/Form Elements/Validators/Stories/Constraint Validators/Radio Button Group" component={RdsRadioButtonGroup} />

# Radio Button Group

### Description List: Required

<Canvas>
  <Story
    name="Description List: Required"
    loaders={[
      () => ({
        buttonRef: React.createRef(),
      }),
    ]}
  >
    {(args, { loaded: { buttonRef } }) => (
      <form onSubmit={e => e.preventDefault()}>
        <rds-radio-button-group label="Access Type" required="true" validate>
          <rds-radio-button for="public-access" label="Public access">
            <rds-sub-label slot="sub-label">This project is fully available to anyone in the organization.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="link-access" label="Link access">
            <rds-sub-label slot="sub-label">This project is available to anyone in the organization with a link.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="private-access" label="Private access">
            <rds-sub-label slot="sub-label">This project is available to users in the organization that were given access.</rds-sub-label>
          </rds-radio-button>
        </rds-radio-button-group>
        <br />
        <rds-button ref={buttonRef} type="submit" onClick={() => buttonRef.current.isFormValid()}>
          'Submit' the form to validate an empty radio button group
        </rds-button>
      </form>
    )}
  </Story>
</Canvas>

### Default: Required

<Canvas>
  <Story
    name="Default: Required"
    loaders={[
      () => ({
        buttonRef: React.createRef(),
      }),
    ]}
  >
    {(args, { loaded: { buttonRef } }) => (
      <form onSubmit={e => e.preventDefault()}>
        <rds-radio-button-group label="Access Type" required="true" validate type="default">
          <rds-radio-button for="public-access" label="Public access">
            <rds-sub-label slot="sub-label">This project is fully available to anyone in the organization.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="link-access" label="Link access">
            <rds-sub-label slot="sub-label">This project is available to anyone in the organization with a link.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="private-access" label="Private access">
            <rds-sub-label slot="sub-label">This project is available to users in the organization that were given access.</rds-sub-label>
          </rds-radio-button>
        </rds-radio-button-group>
        <br />
        <rds-button ref={buttonRef} type="submit" onClick={() => buttonRef.current.isFormValid()}>
          'Submit' the form to validate an empty radio button group
        </rds-button>
      </form>
    )}
  </Story>
</Canvas>

### Small Card: Required

<Canvas>
  <Story
    name="Small Card: Required"
    loaders={[
      () => ({
        buttonRef: React.createRef(),
      }),
    ]}
  >
    {(args, { loaded: { buttonRef } }) => (
      <form onSubmit={e => e.preventDefault()}>
        <rds-radio-button-group label="Access Type" required="true" validate type="small-card">
          <rds-radio-button for="public-access" label="Public access">
            <rds-sub-label slot="sub-label">This project is fully available to anyone in the organization.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="link-access" label="Link access">
            <rds-sub-label slot="sub-label">This project is available to anyone in the organization with a link.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="private-access" label="Private access">
            <rds-sub-label slot="sub-label">This project is available to users in the organization that were given access.</rds-sub-label>
          </rds-radio-button>
        </rds-radio-button-group>
        <br />
        <rds-button ref={buttonRef} type="submit" onClick={() => buttonRef.current.isFormValid()}>
          'Submit' the form to validate an empty radio button group
        </rds-button>
      </form>
    )}
  </Story>
</Canvas>

### Card: Required

<Canvas>
  <Story
    name="Card: Required"
    loaders={[
      () => ({
        buttonRef: React.createRef(),
      }),
    ]}
  >
    {(args, { loaded: { buttonRef } }) => (
      <form onSubmit={e => e.preventDefault()}>
        <rds-radio-button-group label="Access Type" required="true" validate type="card">
          <rds-radio-button for="public-access" label="Public access">
            <rds-sub-label slot="sub-label">This project is fully available to anyone in the organization.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="link-access" label="Link access">
            <rds-sub-label slot="sub-label">This project is available to anyone in the organization with a link.</rds-sub-label>
          </rds-radio-button>
          <rds-radio-button for="private-access" label="Private access">
            <rds-sub-label slot="sub-label">This project is available to users in the organization that were given access.</rds-sub-label>
          </rds-radio-button>
        </rds-radio-button-group>
        <br />
        <rds-button ref={buttonRef} type="submit" onClick={() => buttonRef.current.isFormValid()}>
          'Submit' the form to validate an empty radio button group
        </rds-button>
      </form>
    )}
  </Story>
</Canvas>

_Built for Resilience Design System @ FM Global_
